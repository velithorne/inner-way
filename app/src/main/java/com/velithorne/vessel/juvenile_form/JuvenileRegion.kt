package com.velithorne.vessel.juvenile_form

/**
 * Architectural regions of the first post-seed juvenile body (not pod-centric labels only).
 */
enum class JuvenileRegion {
    CROWN_REGION,
    CORE_REGION,
    LATERAL_REGION_LEFT,
    LATERAL_REGION_RIGHT,
    RESERVE_REGION,
    SHELL_REGION,
    SUPPORT_REGION,
    ARCHIVE_REGION,
    SCAR_OVERLAY,
    REROUTE_OVERLAY,
}
