package com.velithorne.innerway.mind

/**
 * Interpreted internal modes derived from body truth + memory (Phase 4 will expand rules).
 */
enum class InternalState {
    CALM,
    HUNGRY,
    STRESSED,
    DISTURBED,
    RECOVERING,
    RESTING,
    DORMANT,
    ALERT,
    CURIOUS,
    DEFENSIVE,
}
