package com.velithorne.innerway.mind

/**
 * Staged development; unlock logic lives in [com.velithorne.innerway.mind.EvolutionEngine] (Phase 7).
 */
enum class GrowthStage {
    SEED,
    INFANT,
    CHILD,
    ADOLESCENT,
    MATURE,
}
